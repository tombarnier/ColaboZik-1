const feathers = require('@feathersjs/feathers');
const addMusicPlaylist = require('../../src/hooks/add-music-playlist');

describe('\'addMusicPlaylist\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      after: addMusicPlaylist()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
